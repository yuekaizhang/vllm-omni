# Qwen3-Omni-MoE — HYBRID connector: CudaIPC on the prefill edge, SHM on the
# streaming codec edge.
#
# Motivation (H20 finding): CudaIPC's device-to-device handoff helps the one-shot
# thinker->talker prefill transfer (~16-33MB emb+hidden, improves first-packet
# latency) but REGRESSES the many small talker->code2wav streaming-chunk transfers
# on H20 (per-chunk D2D + IPC-ring sync cost dominates, throughput -23%..-40%).
#
# This profile keeps the first-packet win without paying the per-chunk cost:
#   stage 0 -> stage 1 (prefill handoff)      : CudaIPCConnector
#   stage 1 -> stage 2 (streaming codec chunks): SharedMemoryConnector
#
# One stage per GPU (0/1/2), matching qwen3_omni_moe_cuda_ipc.yaml.
#
# Usage:
#   CUDA_VISIBLE_DEVICES=0,1,2 vllm serve <model> --omni --async-chunk \
#       --deploy-config vllm_omni/deploy/qwen3_omni_moe_hybrid_ipc.yaml --port 8090
async_chunk: true

connectors:
  connector_of_cuda_ipc:
    name: CudaIPCConnector
    extra:
      tensor_lifetime_sec: 30.0
      pool_size_mb: 4096
      pool_credits: 64               # slot = 4096/64 = 64MB
      inline_threshold_bytes: 16384  # < 16KB -> inline
      ring_body_max: 524288
      ring_entries: 2048
      initial_codec_chunk_frames: 4
      codec_chunk_frames: 25
      codec_left_context_frames: 25
  connector_of_shared_memory:
    name: SharedMemoryConnector
    extra:
      initial_codec_chunk_frames: 4
      codec_chunk_frames: 25
      codec_left_context_frames: 25

stages:
  - stage_id: 0
    max_num_batched_tokens: 32768
    max_num_seqs: 64
    gpu_memory_utilization: 0.9
    trust_remote_code: true
    enable_prefix_caching: false
    devices: "0"
    output_connectors:
      to_stage_1: connector_of_cuda_ipc
    default_sampling_params:
      temperature: 0.0
      max_tokens: 2048

  - stage_id: 1
    max_num_batched_tokens: 32768
    max_num_seqs: 64
    gpu_memory_utilization: 0.6
    trust_remote_code: true
    enable_prefix_caching: false
    devices: "1"
    input_connectors:
      from_stage_0: connector_of_cuda_ipc          # prefill handoff: D2D
    output_connectors:
      to_stage_2: connector_of_shared_memory       # codec stream: host SHM
    default_sampling_params:
      temperature: 0.9
      top_k: 50
      max_tokens: 4096
      seed: 42
      repetition_penalty: 1.05

  - stage_id: 2
    max_num_batched_tokens: 65536
    max_num_seqs: 64
    gpu_memory_utilization: 0.5
    enforce_eager: false
    trust_remote_code: true
    enable_prefix_caching: false
    enable_chunked_prefill: false
    async_scheduling: false
    devices: "2"
    input_connectors:
      from_stage_1: connector_of_shared_memory     # codec stream: host SHM
    default_sampling_params:
      temperature: 0.0
      top_p: 1.0
      top_k: -1
      max_tokens: 65536
      seed: 42
      repetition_penalty: 1.1
